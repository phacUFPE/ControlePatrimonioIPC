<%= form_with(model: group, local: true) do |form| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <br />

  <%= form.label :group_cod %>
  <%= form.number_field :id, class: "form-control", placeholder: "1" %>

  <br />

  <%= form.label :name %>
  <%= form.text_field :name, id: :group_name, class: "form-control", placeholder: "Sound equipment" %>

  <br />

  <%= form.label :description %>
  <%= form.text_area :description, id: :group_description, class: "form-control", placeholder: "Description" %>

  <br />

  <div class="actions">
    <%= form.submit "Create Group", class: "btn btn-success" %>
  </div>
<% end %>
