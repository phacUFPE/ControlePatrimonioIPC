<h1><%= translate 'users.new' %></h1>

<%= render 'form', user: @user %>

<br />

<%= link_to raw("<span class='glyphicon glyphicon-arrow-left'></span>&nbsp;&nbsp;#{translate 'actions.back'}"), users_path, class: "btn btn-info" %>

